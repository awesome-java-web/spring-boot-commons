<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.awesome.springboot.commons.data.mybatis.interceptor.MybatisTestMapper">
    <resultMap id="userResultMap" type="com.github.awesome.springboot.commons.data.mybatis.interceptor.MybatisTestEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
        <result column="email" property="email"/>
        <result column="city" property="city"/>
        <result column="country" property="country"/>
    </resultMap>

    <update id="createTable">
        CREATE TABLE IF NOT EXISTS `user` (
        `id` BIGINT(20) NOT NULL AUTO_INCREMENT,
        `name` VARCHAR(255) DEFAULT NULL,
        `age` INT(11) DEFAULT NULL,
        `email` VARCHAR(255) DEFAULT NULL,
        `city` VARCHAR(255) DEFAULT NULL,
        `country` VARCHAR(255) DEFAULT NULL,
        PRIMARY KEY (`id`)
        )
    </update>

    <insert id="insertUser" parameterType="com.github.awesome.springboot.commons.data.mybatis.interceptor.MybatisTestEntity">
        INSERT INTO `user` (name, age, email, city, country) VALUES (#{name}, #{age}, #{email}, #{city}, #{country})
    </insert>

    <select id="selectUser" resultType="com.github.awesome.springboot.commons.data.mybatis.interceptor.MybatisTestEntity">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <update id="updateUser">
        UPDATE `user` SET name = #{name}, age = #{age}, email = #{email}, city = #{city}, country = #{country} WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM `user` WHERE id = #{id}
    </delete>
</mapper>
